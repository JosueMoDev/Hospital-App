{
  "name": "clinic-node-api",
  "version": "1.0.0",
  "main": "app.ts",
  "repository": "https://github.com/JosueMoDev/clinic-node-api.git",
  "author": "Jonas Morales <josuemodev@gmail.com>",
  "scripts": {
    "dev": "tsnd --respawn --clear src/app.ts",
    "build": "rimraf ./dist && tsc",
    "start": "yarn run build && node dist/app.js",
    "docker:test": "docker compose -f docker-compose-test.yml --env-file .env.test up -d",
    "test": " yarn run docker:test && jest",
    "test:watch": " yarn run docker:test && jest --watch",
    "test:coverage": "yarn run docker:test && jest --coverage"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^29.5.6",
    "@types/node": "^20.8.7",
    "jest": "^29.7.0",
    "rimraf": "^5.0.5",
    "supertest": "^6.3.3",
    "ts-jest": "^29.1.1",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.2.2",
    "prisma": "^5.5.2"

  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "@prisma/client": "5.5.1",
    "compression": "^1.7.4",
    "env-var": "^7.4.1",
    "express": "^4.18.2"
  }
}
